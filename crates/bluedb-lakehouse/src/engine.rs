//! `LakehouseEngine` — owns the opt-out registry, the event-driven seal loop, and the
//! compaction worker; reopens from durable state on promote (spec §3, §5, §8). Built out in
//! Phase 3.

/// The lakehouse mirror engine. Placeholder scaffold — fields and lifecycle land in Phase 3.
pub struct LakehouseEngine;
